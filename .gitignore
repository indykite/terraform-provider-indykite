# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

terraform-provider-indykite

# Test binary, built with `go test -c`
*.test

# Integration tests with locally build Terraform provider
tests/provider/.terraform
tests/provider/terraform.d
tests/provider/.terraform.lock.hcl
tests/provider/terraform.tfstate
tests/provider/terraform.tfstate.backup

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/
dist

.idea
.claude
/local/

# Mac OS
.DS_Store
